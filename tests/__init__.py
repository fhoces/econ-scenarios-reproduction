"""The validation suite: the paper's published tables, the paths, the statics, the steady
state, the simulation identities, printed precision, the explorer grid and the numbers typed
into the prose, every one recomputed from aiscen. Run with `python3 -m pytest -q`."""
