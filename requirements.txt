# Dependencies for rendering repro.qmd, and nothing else. The aiscen package, its tests,
# run.py and the exporters in slides/ are all standard-library only; see README.md.
# No strict pins: any reasonably recent release of each should work.
pandas
matplotlib
tabulate
