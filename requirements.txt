# Dependencies for the report path only (repro.qmd, run.py --csv, slides/make_grid_app.py).
# The aiscen package itself is standard-library only; see README.md.
# No strict pins: any reasonably recent release of each should work.
pandas
matplotlib
tabulate
